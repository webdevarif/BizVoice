import React from 'react';
import ReactDOM from 'react-dom/client';
import { Update } from './Update';
import '../styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<Update />);
