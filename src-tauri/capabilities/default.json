{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window APIs for all app windows.",
  "windows": ["login", "settings", "update", "micbar"],
  "comment": "store:default is deliberately NOT granted: the WebView reaches settings only through the get_settings/set_settings commands, which mask ElevenLabs keys. Direct store access would let the WebView read the raw ciphertext and defeat that masking.",
  "permissions": ["core:default", "updater:default"]
}
